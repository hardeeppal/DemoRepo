package FunctionalProgramming;

public interface Answerable {
    // Functional programming focuses on computing results from functions rather than performing action on objects
    // We make class blueprint before but on here objects are immutable instead of changing  a new object
    // you have to create new object which like old object
    String answer();
}
